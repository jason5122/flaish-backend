export type ThemeColors = {
  background: string;
  text: string;
  primary: string;
  secondary: string;
  tertiary: string;
  accent: string;
};

export type ThemeType = {
  light: ThemeColors;
  dark: ThemeColors;
};
